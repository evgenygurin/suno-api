# 🎵 Как скачать свой трек из Suno API

## 📋 У тебя есть:

- ✅ Task ID трека: `b9247557-4a34-4a3f-8c9e-8023eee78d08`
- ✅ Скрипт для скачивания: `download_track.sh`

## ⚠️ Проблема:

API ключи, которые ты использовал, не работают (401 ошибка):
- `52082f4fc871b6231ae719899fe53d02` ❌
- `59f5f8273b52677e5df9bb477155fabb` ❌

## 🔑 Решение:

### Вариант 1: Получить правильный API ключ

1. **Откуда ты создавал трек?**
   - Если через веб-интерфейс https://sunoapi.org - найди свой API ключ в личном кабинете
   - Если через другой сервис - проверь документацию

2. **Получить новый API ключ:**
   - Перейди на https://sunoapi.org/api-key
   - Зарегистрируйся или войди
   - Скопируй свой API ключ

### Вариант 2: Использовать готовый скрипт

```bash
# После получения правильного API ключа:
./download_track.sh b9247557-4a34-4a3f-8c9e-8023eee78d08 YOUR_REAL_API_KEY
```

Скрипт автоматически:
- ✅ Проверит статус трека
- ✅ Покажет информацию о треке (название, длительность, стиль)
- ✅ Скачает MP3 файл
- ✅ Скачает видео (если есть)

## 📊 Проверка статуса вручную

Если хочешь просто проверить статус без скачивания:

```bash
curl -X GET \
  "https://api.sunoapi.org/api/v1/generate/record-info?taskId=b9247557-4a34-4a3f-8c9e-8023eee78d08" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  | jq .
```

### Возможные статусы:

- **PENDING** / **GENERATING** - Трек ещё создаётся (подожди 1-3 минуты)
- **SUCCESS** - Трек готов! В ответе будет `audio_url` для скачивания
- **FAILED** - Ошибка генерации

## 💾 Прямое скачивание (когда трек готов)

Когда статус станет `SUCCESS`, в ответе API будет:

```json
{
  "code": 200,
  "data": {
    "status": "SUCCESS",
    "response": {
      "data": [
        {
          "id": "...",
          "title": "Дрилл-Частушка — Голубь и Басс",
          "audio_url": "https://...",
          "video_url": "https://...",
          "duration": 180
        }
      ]
    }
  }
}
```

Просто открой `audio_url` в браузере или скачай:

```bash
curl -o "Дрилл-Частушка.mp3" "AUDIO_URL_FROM_RESPONSE"
```

## 🔍 Найти свой API ключ

Если ты создавал трек, значит где-то использовал API ключ. Проверь:

1. **В .env файле этого проекта:**
   ```bash
   cat .env | grep SUNO_API_KEY
   ```

2. **В истории команд терминала:**
   ```bash
   history | grep "Authorization: Bearer"
   ```

3. **В curl файлах:**
   ```bash
   cat curl_*.sh
   ```

4. **В переменных окружения:**
   ```bash
   echo $SUNO_API_KEY
   ```

## 🎯 Быстрый чеклист:

- [ ] Найти правильный API ключ
- [ ] Запустить `./download_track.sh <TASK_ID> <API_KEY>`
- [ ] Подождать если статус GENERATING
- [ ] Трек скачается автоматически!

---

## 💡 Примечание

Трек с таким task_id уже создан и существует в системе Suno. Тебе нужен только правильный API ключ чтобы его скачать!

**Твой трек:** "Дрилл-Частушка — Голубь и Басс"
**Стиль:** Russian drill-rap с частушками, балалайкой и 808
**Task ID:** b9247557-4a34-4a3f-8c9e-8023eee78d08

Удачи! 🎶
